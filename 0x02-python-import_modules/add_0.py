#!/usr/bin/python3
def add(a, b):
    """My addition function

    Args:
        a: first integer
        b: second integer

    Returns:
        The return value. a + b
    """
    a = int(a)
    b = int(b)
    sum = a + b
    print("{:d} + {:d} = {:d}.format(a, b. sum)")
    return (sum)
